services:
  zeroclaw-gateway:
    image: ${IMAGE:-zeroclaw-test:latest}
    build:
      context: .
      dockerfile: Dockerfile
    platform: ${PLATFORM:-linux/amd64}
    container_name: ${STABLE_HOSTNAME:-zeroclaw-test}
    env_file: .env
